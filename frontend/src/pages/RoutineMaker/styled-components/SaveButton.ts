import styled from 'styled-components';

export const SaveButton = styled.button`
  padding: 0.25rem;
  background-color: var(--boton-save);
  border: 1px solid black;
  border-radius: 0.5rem;

  &:hover {
    filter: contrast(120%);
  }
`;
