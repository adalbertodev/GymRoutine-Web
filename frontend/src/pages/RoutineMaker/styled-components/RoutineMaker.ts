import { Button } from '@mui/material';
import styled from 'styled-components';

export const RoutineMakerContainer = styled.div`
  width: 80%;
  margin: auto;
`;

export const AddButtonRow = styled(Button)``;
